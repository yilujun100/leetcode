import climbStairs from '../../code/math/70.climbStairs';

test('climbing stairs', () => {
	expect(climbStairs(2)).toBe(2);
});

test('climbing stairs 1:', () => {
	expect(climbStairs(3)).toBe(3);
});
